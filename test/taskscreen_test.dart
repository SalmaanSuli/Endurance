import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:endurance_fitness/tasksscreen.dart';

void main() {
  testWidgets('Add task icon', (WidgetTester tester) async {
    //await tester.pumpWidget(const MyTasksWidget());

    //await tester.tap(find.byIcon(Icons.add));
    //await tester.pump();

    //expect(find.text("Add Task"), findsOneWidget);
  });

  testWidgets('Showing results for  all goals', (WidgetTester tester) async {
    //await tester.pumpWidget(const MyTasksWidget());

    //expect(find.text('Showing results for  all goals'), findsOneWidget);
  });

  testWidgets('Deleting task', (WidgetTester tester) async {
    //await tester.pumpWidget(const MyTasksWidget());
  });

  testWidgets('Adding tasks', (WidgetTester tester) async {
    //await tester.pumpWidget(const MyTasksWidget());
  });
}
